(a++)++ -> 加在了拷贝上，故只进行了1
(++a)++ -> 引用，故进行了2次

拷贝，引用的返回

拷贝构造重载

链式调用cin >> a >> b;
前提：返回的是对象本人
cin.ger().get()
cout.put().put()

菱形问题

override？

peek
get
getline
cin.getline(char [],100)
getline(cin,string s )?

文件读写

二、和三对应

常函数是常成员唯一的对外接口

const可以作为函数重载的区分

模板的填空

AA* p1,p2,
都是对象
AA *p1,p2

show(con C){}
Con c1
show(c1)会调用一次拷贝构造

注意赋值运算符和拷贝构造
class A{};空对象的运用
拷贝，构造，重载，析构

含有子对象和基类，

缺省构造

调用基类构造函数的方式
显式调用，隐式调用

静态成员

class{
    void {static int score = }
}

文件流？
bigtree5566\n
clear
ignore